# Required only when database-backed functions are intentionally enabled.
# Never infer the target environment from the presence of this variable alone.
NETLIFY_DB_URL=

# Optional. Use "server" for the node-postgres driver where appropriate.
NETLIFY_DB_DRIVER=
